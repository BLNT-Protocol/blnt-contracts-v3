[package]
name = "mock-pool-factory"
version = "0.0.0"
authors = ["Blend Capital <gm@blend.capital>"]
license = "AGPL-3.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]
doctest = false

[features]
testutils = ["soroban-sdk/testutils", "mock-pool/testutils"]

[dependencies]
soroban-sdk = { workspace = true }
pool = { path = "../../pool", features = ["testutils"] }
mock-pool = { path = "../mock-pool" }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
rand = {version = "0.7.3"}
